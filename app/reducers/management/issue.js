var initialState = {
  fetching: false,
  response: {
    // todo data
  },
  error: undefined
};

function managementIssue (state = initialState, action) {
  return state;
}

export default managementIssue;
