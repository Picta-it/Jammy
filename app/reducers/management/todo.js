var initialState = {
  fetching: false,
  response: {
    // todo data
  },
  error: undefined
};

function managementTodo (state = initialState, action) {
  return state;
}

export default managementTodo;
