var initialState = {
  fetching: false,
  response: {
    // todo data
  },
  error: undefined
};

function assisteurIssue (state = initialState, action) {
  return state;
}

export default assisteurIssue;
