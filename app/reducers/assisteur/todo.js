var initialState = {
  fetching: false,
  response: {
    // todo data
  },
  error: undefined
};

function assisteurTodo (state = initialState, action) {
  return state;
}

export default assisteurTodo;
