var initialState = {
  fetching: false,
  response: {
    // todo data
  },
  error: undefined
};

function acheteurTask (state = initialState, action) {
  return state;
}

export default acheteurTask;
