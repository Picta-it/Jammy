var initialState = {
  fetching: false,
  response: {
    // todo data
  },
  error: undefined
};

function acheteurTodo (state = initialState, action) {
  return state;
}

export default acheteurTodo;
