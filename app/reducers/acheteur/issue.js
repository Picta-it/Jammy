var initialState = {
  fetching: false,
  response: {
    // todo data
  },
  error: undefined
};

function acheteurIssue (state = initialState, action) {
  return state;
}

export default acheteurIssue;
